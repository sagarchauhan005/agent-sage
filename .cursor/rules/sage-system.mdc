---
description: Sage agent system — always load root Agents.md and route through SDLC agents.
alwaysApply: true
---

# Sage system

This project uses the **Sage** SDLC agent system.

Always read [Agents.md](../../Agents.md) first. It is the root contract linked to all phase agents in `agents/`.

When doing SDLC work, use `/sage-*` slash commands. Do not skip orchestrator handoffs.

Agent registry and flow: see the top of Agents.md.
