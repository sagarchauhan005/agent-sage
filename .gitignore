.DS_Store
.env
.shopify-agent/
__pycache__/
*.pyc
.pytest_cache/
node_modules/
*.tgz

# Sage run artifacts (this tool repo ships templates only; real runs belong
# in the consumer project, not here).
runs/*/
runs/.current
!runs/_plan-template.md
!runs/_manifest-template.json
!runs/_manifest-template.schema.json
