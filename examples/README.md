# Examples

Worked runs showing the expected density and format for each artifact. These are reference material, not live runs — copy the structure, not the content.

| Example | Profile | Shows |
|---------|---------|-------|
| [hotfix-rate-limit-message/](hotfix-rate-limit-message/) | `hotfix` | Shortest pipeline (plan → build → qa → release), condensed three-layer plan, manifest with `design`/`architect`/`devops` skipped |

For a full pipeline (`web-product`, with design and architect), use this example's `plan.md` for density and add a `design.md` / `architecture.md` of similar length — see [agents/sage-designer.md](../agents/sage-designer.md) and [agents/sage-architect.md](../agents/sage-architect.md) for their output sections.
