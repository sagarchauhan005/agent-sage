# Sage

Read [Agents.md](Agents.md) first. It is the root contract for the Sage SDLC agent system: agent registry, workflow profiles, stack preferences, and global rules.

Use `/sage-*` slash commands for SDLC work (e.g. `/sage-orchestrator`, `/sage-plan`, `/sage-engineer`, `/sage-qa`, `/sage-release`).
